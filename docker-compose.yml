services:
  krakend:
    image: krakend:2.10.0
    container_name: krakend-gateway
    ports:
      - "8080:9000"
    volumes:
      - ./krakend.json:/etc/krakend/krakend.json:ro
    networks:
      - general-network

networks:
  general-network:
    driver: bridge
